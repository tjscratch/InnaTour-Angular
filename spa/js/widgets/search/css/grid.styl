.b-inna-row{
	position relative
	margin-right -2px
	margin-left -2px
}

for key in (1..12){
	$cols = 12 / key
	.b-inna-col-xs-{key}{
		position: relative;
		float: left;
		width: (100 / $cols) %;
		min-height: 1px;
		box-sizing: border-box;
		padding-left: 2px;
		padding-right: 2px;
	}
}