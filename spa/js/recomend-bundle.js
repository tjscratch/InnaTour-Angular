$(function () {
    var isCollapse = false;
    var recommendBundle = $('.recommend-bundle');
    var scrollTop

    if (recommendBundle.length) {
        isCollapse

        scrollTop = $(document.body).scrollTop();

        $(window).on('scroll', function (evt) {


            if (scrollTop > 20) {

            }
        });

    }

    function collapse () {

    }

    function expand () {

    }
});