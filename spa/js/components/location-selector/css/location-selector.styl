.b-location_selector{
  position: relative;

  .ui-tooltip{
    display: block;
    position: absolute;
    left: 0;
    top: 65px;
    max-width: inherit;

    .ui-tooltip-content{
      white-space: nowrap;
    }
  }
}