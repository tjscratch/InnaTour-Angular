﻿
'use strict';

/* Directives */

innaAppDirectives.
    directive('autoCompleteCustom', ['$parse', function ($parse) {
        return {
            restrict: 'A',
            link: function (scope, element, attrs) {
                
            }
        };
    }]);