.partner-wl {
    /*развернутая шапка*/
    .search-form-expanded {
        .header-menu {
            padding-top 150px;
            background-color transparent;
        }

        .header__nav li a {
            color: #87bfe1;
            background-color $backColorAlfa;

            &:hover {
                color: #fff;
            }

            &.active {
                color: #fff;
                background-color $backColor;
            }
        }

        .b-search-form, .search-form-expanded .search-form {
            background-color $backColor;
        }

        .header__user {
            background-color $backColorAlfa;
        }
    }

    /*свернутая шапка*/
    .header__container {
        padding 0;
    }

    .header__user {
        background-color $backColorAlfa;

        .header__user-nav {
            padding: 8px 6px 6px 0px;
        }
    }

    .header-menu {
        padding 0;
        background-color #fff;
    }

    .header__nav {
        li {
            margin 0 3px;

            &:first-child {
                margin-left 0;
            }

            a {
                background-color $backColorAlfa;

                &.active {
                    background-color $backColor;
                }
            }
        }
    }

    .b-search-form {
        background-color $backColor;
    }

    /*datepicker*/
    .datepickerSelected-start a
    .datepickerSelected-end a {
        color-datepicker-background-wl()
    }

    .datepickerSelected a {
        color: #fff!important;
        color-datepicker-bg();
    }

    .datePicker-today a {
        color-border-wl()
    }

    .datepickerDays a {

        &:hover {
            color-datepicker-background-wl()
        }
    }

    .datepickerGoPrev a
    .datepickerGoNext a {
        &:hover {
            color-datepicker-background-wl()
        }
    }

    /*выбор людей*/
    .search-form-dialog-row .button-group .button.active {
        color-datepicker-bg();
    }

    /*кнопка найти*/
    .search-form__submit {
        color-bg-btn-wl()
    }
}