{{#if userData}}
	<script type="application/javascript">
		var userData = {};

			{{!--#each userData}}
				userData['{{@key}}'] = '{{this}}';
			{{/each--}}

		userData = {
			{{#if userData.AgencyActive}}
				"AgencyActive": {{userData.AgencyActive}},
			{{/if}}
			{{#if userData.AgencyId}}
				"AgencyId": {{userData.AgencyId}},
			{{/if}}
			{{#if userData.AgencyName}}
				"AgencyName": "{{userData.AgencyName}}",
			{{/if}}
			{{#if userData.Email}}
				"Email": "{{userData.Email}}",
			{{/if}}
			{{#if userData.FirstName}}
				"FirstName": "{{userData.FirstName}}",
			{{/if}}
			{{#if userData.IsSocial}}
				"IsSocial": {{userData.IsSocial}},
			{{/if}}
			{{#if userData.LastName}}
				"LastName": "{{userData.LastName}}",
			{{/if}}
			{{#if userData.MessagesCount}}
				"MessagesCount": {{userData.MessagesCount}},
			{{/if}}
			{{#if userData.Phone}}
				"Phone": "{{userData.Phone}}",
			{{/if}}
			{{#if userData.SupportPhone}}
				"SupportPhone": {{userData.SupportPhone}},
			{{/if}}
			{{#if userData.Type}}
				"Type": {{userData.Type}}
			{{/if}}
		};
	</script>
{{/if}}